import React from 'react'
import styled from 'styled-components'

const StyledSideBar = styled.main.attrs({
    className: ''
})` `

export default function SideBar() {
    return (
        <StyledSideBar></StyledSideBar>
    )
}
